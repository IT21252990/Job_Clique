<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/registerbg"
    tools:context=".MainActivityRegistration">

    <Button
        android:id="@+id/btnJobSeekerRegi"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:background="@color/Blue"
        android:drawableBottom="@drawable/icons8_job_seeker_48"
        android:drawablePadding="-10dp"
        android:onClick="ReqApproval"
        android:text="Register as a Job Seeker"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btnEmployerRegi" />

    <Button
        android:id="@+id/btnEmployerRegi"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:background="@color/Blue"
        android:drawableBottom="@drawable/icons8_employer_64"
        android:drawablePadding="-10dp"
        android:onClick="ReqApproval"
        android:text="Register as an Employer"
        android:textColor="@color/white"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/btnJobSeekerRegi"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>